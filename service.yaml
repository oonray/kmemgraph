apiVersion: v1
kind: Service
metadata:
  name: memgraph
spec:
  selector:
    app.kubernetes.io/name: memgraph
  ports:
    - protocol: TCP
      name: "7474"
      port: 7474
      targetPort: 7474
    - protocol: TCP
      name: "3000"
      port: 3000
      targetPort: 3000
